services:
  glance:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./boards.json:/app/boards.json
      - ./gantt-order.json:/app/gantt-order.json
    restart: unless-stopped
